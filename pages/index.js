import Layout from '../components/layout'
import Link from 'next/link'
// when getting the random art, rearrange the list and show 10 at a time
export default function HomePage() {
    return (
        <Layout pageTitle="Home">
          <h1>Hello</h1>
        </Layout>
    );
}